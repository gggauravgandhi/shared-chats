---
interface Props {
  data: object | object[];
}

const { data } = Astro.props;
const items = Array.isArray(data) ? data : [data];
---

{items.map((obj) => <script is:inline type="application/ld+json" set:html={JSON.stringify(obj)} />)}

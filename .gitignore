# build output
dist/
.astro/

# dependencies
node_modules/

# environment
.env
.env.*

# OS
.DS_Store

# source transcript archive — must NEVER be committed (see CLAUDE.md / spec §1.3)
temp_data_repo/

# artifacton.com mirror (60MB of recovered HTML — local backup, not site content)
data/artifacts/

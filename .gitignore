# build output
dist/
.astro/

# dependencies
node_modules/

# environment
.env
.env.*

# OS
.DS_Store

# source transcript archive — must NEVER be committed (see CLAUDE.md / spec §1.3)
temp_data_repo/

# local working data for artifact curation — never committed
data/artifacts/
